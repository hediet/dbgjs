[package]
name = "cdp-protocol"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
linkrpc = { version = "0.1.0", path = "../../../linkrpc/rust/crates/linkrpc" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[build-dependencies]
linkrpc = { version = "0.1.0", path = "../../../linkrpc/rust/crates/linkrpc" }
serde_json = "1"
thiserror = "2"
